This application help's build the pages dynamically.

Based on the Json object 'stdPageTemplate' which is placed in app.js file.

Propertise for each field of elements can be configured and validation can be applied.

By using this the page the html generated file is save and on applying Css this is ready to use.

This helped a lot for serving various SaSa based client page. which use to take weeks to get tested and deployed.

Next step Working on UI Grid based approach to configure the propertise and get the JSON out of it. 
